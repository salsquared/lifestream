/** P0.7.4 placeholder. The world map is drawn in P2 from the vendored topojson (P0.6). */
export function MapView() {
  return (
    <div className="view-stub">
      <span className="view-stub__title">Map</span>
      <span className="view-stub__note">Empty until P2 — d3-geo + topojson → SVG.</span>
    </div>
  );
}
