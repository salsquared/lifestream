/** P0.7.4 placeholder. The 3D corridor arrives in P4; this route is why P0.7.2 defaults to /v/map. */
export function TimelineView() {
  return (
    <div className="view-stub">
      <span className="view-stub__title">Time Corridor</span>
      <span className="view-stub__note">Empty until P4 — events on a 3D time axis.</span>
    </div>
  );
}
