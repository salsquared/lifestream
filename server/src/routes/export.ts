/**
 * `/api/export` — export manifests, document builders and renderers.
 *
 * Placeholder. Lands in P15 (export pipeline, HTML). See architecture.html §8.
 */
import { Hono } from 'hono';

export const exportRoutes = new Hono();
