/**
 * `/api/saves` — save list / create / fork / dump / import.
 *
 * Placeholder. Lands in P6 (saves UI & the fork). Until then the active save is a
 * hard-coded constant (P1.13). See architecture.html §2.6 (`forkSave`) and §7.4.
 */
import { Hono } from 'hono';

export const saveRoutes = new Hono();
